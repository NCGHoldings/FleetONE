name: Run Magiya Scraper

on:
  schedule:
    # Run every day at 11:59 PM UTC
    - cron: '59 23 * * *'
  workflow_dispatch: # Allows you to run this manually from the GitHub UI right now!

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: |
          npm install --force --legacy-peer-deps
          # Ensure puppeteer is definitely installed
          PUPPETEER_SKIP_DOWNLOAD=true npm install puppeteer@21.11.0 dotenv pdf.js-extract @supabase/supabase-js --no-save --force

      - name: Run Scraper
        env:
          VITE_SUPABASE_URL: https://wsomdfqnkxwcptilsifr.supabase.co
          SUPABASE_SERVICE_ROLE_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Indzb21kZnFua3h3Y3B0aWxzaWZyIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2MDM3ODQ5NCwiZXhwIjoyMDc1OTU0NDk0fQ.T3rbhdNcYw9N19Sg45GyqlM_x-AV7FD4_8KQfD1Tf6U
        run: node scripts/magiya_scraper.js
