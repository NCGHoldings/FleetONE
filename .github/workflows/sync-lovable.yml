name: Sync to Lovable
on:
  push:
    branches: [] # Temporarily disabled: target repo inaccessible
  workflow_dispatch:  # Allows manual trigger

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Golden Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to Lovable Server
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          # Strip any accidental hidden newlines or spaces from the copied token
          CLEAN_TOKEN=$(echo "$GH_PAT" | tr -d '\n' | tr -d '\r' | tr -d ' ')
          
          # Add remote securely referencing the correct globallyceum branch
          git remote add lovable "https://${CLEAN_TOKEN}@github.com/globallyceum25-dot/ncg-fleetone-545c8dda.git"
          
          git push -f lovable main
